# Maze-Solver
Maze Solver is a learning project from Boot Dev.

The project creates a randomly generated maze from set constants, then tries to solve it with an algorithm. It is visually shown with Tkinter.
